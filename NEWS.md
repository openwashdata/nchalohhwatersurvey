# nchalohhwatersurvey 1.0.1

* First release that carries the data: the v1.0.0 tag points at the initial commit, so the v1.0.0 Zenodo record holds only the package skeleton.
* Metadata: CITATION.cff and inst/CITATION carry the Zenodo concept DOI and declare the work as a dataset; DESCRIPTION gains keywords and spatial and temporal coverage; the pkgdown site is configured per the openwashdata standard and deployed from gh-pages; R CMD check runs in CI. The data are unchanged.

# nchalohhwatersurvey 1.0.0

* First release on Zenodo (tag on the initial commit, without the data).
